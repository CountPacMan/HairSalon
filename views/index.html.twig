<!DOCTYPE html>
<html>
  <head>
    <link rel='stylesheet' href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/styles.css">
    <title>Horrific Cuts Hair Salon</title>
  </head>
  <body>

    <div class='container'>
      <h1>Horrific Cuts Hair Salon</h1>

      <p>Add a new stylist</p>

      <form action='/stylists' method='post'>
        <div class='form-group'>
          <label for='name'>Stylist name:</label>
          <input id='name' name='name' class='form-control' type='text'>
        </div>
        <button type='submit' class='btn btn-success'>Submit</button>
      </form>

      {% if stylists is not empty %}
        <h4>Stylist list</h4>
        <h6>Click on x to delete</h6>
        <ul>
          {% for stylist in stylists %}
            <li>
              <form action='/stylists/{{ stylist.getId }}' method='post' class='inlineForm'>
                <input name='_method' type='hidden' value='delete'>
                <input type='submit' class='deleteLink' value='x'>
              </form>
              <a href="/stylists/{{ stylist.getId }}">{{ stylist.getName }}</a>
            </li>
          {% endfor %}
        </ul>
        {% else %}
          <p>There are no stylists associated with this salon.</p>
      {% endif %}
    </div>

  </body>
</html>
